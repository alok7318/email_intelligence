from setuptools import setup, find_packages

setup(
    name="email_intelligence",
    version="0.0.1",
    description="Email parser for bank transactions, CRM contacts, and quote requests",
    author="Madhav International",
    author_email="sales@madhavinternational.com.np",
    packages=find_packages(),
    zip_safe=False,
    include_package_data=True,
    install_requires=[],
)
